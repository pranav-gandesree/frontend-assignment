'use client' 

// import { useSelector } from 'react-redux';
// import { selectSavedPhotos } from '../../redux/slice';
// import { RootState } from '../../redux/store';

const SavedItems = () => {
  // const savedPhotos = useSelector((state: RootState) => selectSavedPhotos(state));

return(
  <div>
    saveddddd
  </div>
  // savedPhotos.map((photos)=>{
  //   <div>
  //    <p>{photos.title}</p>
  //   </div>
  // })
)
};

export default SavedItems;

